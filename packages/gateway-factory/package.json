{
  "name": "@ai3d/gateway-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Environment-backed LLM provider selection and AiGateway composition.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ai3d/ai-gateway": "workspace:*",
    "@ai3d/plugin-provider-gemini": "workspace:*",
    "@ai3d/plugin-provider-openai": "workspace:*"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "clean": "rimraf dist",
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests",
    "typecheck": "tsc --noEmit"
  }
}
