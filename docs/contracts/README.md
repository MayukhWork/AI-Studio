# Contract Documentation

This directory will hold human-readable references for versioned public contracts. Runtime, tool, agent, plugin, command, query, event, and migration contracts must be documented here as they are introduced.
